# 📘 部署与排错教程

> 面向所有使用本项目的人——从零搭建到常见报错解决。

---

## 目录

1. [快速开始（本地运行）](#1-快速开始本地运行)
2. [Docker 部署](#2-docker-部署)
3. [生产环境部署](#3-生产环境部署)
4. [常见启动错误](#4-常见启动错误)
5. [运行时错误](#5-运行时错误)
6. [自定义修改指南](#6-自定义修改指南)
7. [更新与维护](#7-更新与维护)

---

## 1. 快速开始（本地运行）

### 1.1 环境要求

- Python 3.10 或更高版本
- pip（通常随 Python 一起安装）

### 1.2 克隆项目

```bash
git clone https://github.com/你的用户名/aichat.git
cd aichat
```

### 1.3 创建虚拟环境（推荐）

```bash
# Linux / macOS
python3 -m venv .venv
source .venv/bin/activate

# Windows（PowerShell）
python -m venv .venv
.venv\Scripts\activate
```

### 1.4 安装依赖

```bash
pip install -r requirements.txt
```

### 1.5 生成 SECRET_KEY（必须！）

```bash
# Linux / macOS
echo "SECRET_KEY=$(python3 -c 'import secrets;print(secrets.token_hex(32))')" > .env

# Windows PowerShell
$key = python -c "import secrets;print(secrets.token_hex(32))"
"SECRET_KEY=$key" | Out-File -Encoding utf8 .env
```

### 1.6 启动

```bash
python main.py
```

打开浏览器访问 **http://localhost:3210**，注册账号后即可使用。

---

## 2. Docker 部署

### 2.1 修改 SECRET_KEY

打开 `docker-compose.yml`，把 `SECRET_KEY=请改成随机字符串至少32位` 替换为你的随机密钥：

```yaml
environment:
  - SECRET_KEY=你的32位以上随机字符串
```

生成随机字符串：
```bash
python3 -c 'import secrets;print(secrets.token_hex(32))'
```

### 2.2 启动

```bash
docker-compose up -d
```

访问 **http://localhost:3210**。

### 2.3 数据持久化

`docker-compose.yml` 已经将 `./data` 目录挂载到容器内的 `/app/data`。数据库文件 `data.db` 会保存在宿主机当前目录的 `data/` 下。

### 2.4 停止与更新

```bash
# 停止
docker-compose down

# 更新代码后重新构建并启动
docker-compose up -d --build
```

---

## 3. 生产环境部署

### 3.1 使用 Gunicorn（推荐）

先按第 1 节的步骤装好依赖，然后：

1. **修改 `gunicorn_conf.py`**：把里面的路径改成你服务器上的实际路径：

```python
chdir = '/你的项目路径/aichat'
pidfile = '/你的项目路径/gunicorn.pid'
accesslog = '/你的日志路径/gunicorn_access.log'
errorlog = '/你的日志路径/gunicorn_error.log'
user = '你的用户名'   # 可选
```

2. **启动**：

```bash
gunicorn -c gunicorn_conf.py main:app
```

3. **配置反向代理**（Nginx 示例）：

```nginx
server {
    listen 80;
    server_name your-domain.com;

    location / {
        proxy_pass http://127.0.0.1:3210;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_read_timeout 300s;
        proxy_send_timeout 300s;
    }
}
```

> ⚠️ AI 聊天接口可能响应较慢，`proxy_read_timeout` 务必设大一些（≥300秒）。

### 3.2 使用 uWSGI

修改 `uwsgi.ini` 中的路径后：

```bash
uwsgi --ini uwsgi.ini
```

### 3.3 宝塔面板部署

1. 在宝塔面板中创建 Python 项目
2. 项目路径指向本项目目录
3. 运行文件选择 `main.py`
4. 端口设为 `3210`
5. 在面板中设置环境变量 `SECRET_KEY`
6. 启动项目

### 3.4 使用 systemd 设为开机自启

创建 `/etc/systemd/system/aichat.service`：

```ini
[Unit]
Description=个人学习实验台
After=network.target

[Service]
Type=simple
User=www
WorkingDirectory=/你的项目路径/aichat
Environment="SECRET_KEY=你的密钥"
ExecStart=/你的项目路径/aichat/.venv/bin/gunicorn -c gunicorn_conf.py main:app
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
```

然后：
```bash
sudo systemctl daemon-reload
sudo systemctl enable aichat
sudo systemctl start aichat
sudo systemctl status aichat   # 查看运行状态
```

---

## 4. 常见启动错误

### ❌ `未设置 SECRET_KEY，拒绝启动`

**完整报错**：
```
RuntimeError: !!! 未设置 SECRET_KEY，拒绝启动 !!!
```

**原因**：没有创建 `.env` 文件，或 `.env` 中没有 `SECRET_KEY`。

**解决**：
```bash
# 确认 .env 存在
ls -la .env

# 不存在则创建（参考 1.5 节）
# 确认内容正确
cat .env
# 应该显示: SECRET_KEY=一串很长的随机字符
```

### ❌ `ModuleNotFoundError: No module named 'xxx'`

**原因**：依赖未安装或虚拟环境未激活。

**解决**：
```bash
# 确认虚拟环境已激活（命令行前应有 (.venv) 标记）
# 重新安装依赖
pip install -r requirements.txt
```

### ❌ `Address already in use`（端口被占用）

**完整报错**：
```
OSError: [Errno 98] Address already in use
# 或 Windows: [WinError 10048]
```

**原因**：端口 3210 已被其他程序占用。

**解决**：
```bash
# 方案1：换个端口
PORT=8080 python main.py

# 方案2：查杀占用端口的进程
# Linux
lsof -i :3210
kill -9 <PID>

# Windows
netstat -ano | findstr :3210
taskkill /PID <PID> /F
```

### ❌ Docker 容器启动后立即退出

**原因**：通常是 `SECRET_KEY` 未正确设置。

**解决**：
```bash
# 查看容器日志
docker-compose logs aichat

# 确认 docker-compose.yml 中的 SECRET_KEY 不是占位符
```

### ❌ `sqlite3.OperationalError: unable to open database file`

**原因**：SQLite 所在目录没有写入权限。

**解决**：
```bash
# 确认当前用户对项目目录有写权限
chmod 755 /你的项目路径/aichat
# 如果是 Docker，确认 data 目录存在且可写
mkdir -p data && chmod 777 data
```

---

## 5. 运行时错误

### ❌ 聊天时返回 `检查API KEY的格式是否正确或已过期`

**原因**：未在设置页面填写对应提供商的 API Key，或 Key 已过期/被禁用。

**解决**：
1. 打开 `http://localhost:3210/settings`
2. 在下拉框选择你要用的提供商
3. 填入正确的 API Key，点击保存
4. 回到聊天页重试

### ❌ 登录后很快就提示"登录已过期"

**原因**：服务器重启导致内存中的 Token 黑名单丢失，但新 Token 仍然有效。如果频繁出现，可能是 `SECRET_KEY` 变动了。

**解决**：
- 确保 `.env` 中的 `SECRET_KEY` 没有改动过
- 如果换了 `SECRET_KEY`，所有用户需要重新登录
- Token 有效期默认 30 天，一般不会过期

### ❌ 注册时验证码一直提示错误

**原因**：验证码的 HMAC 签名依赖 `SECRET_KEY`。如果启动后改了 `SECRET_KEY`，之前的验证码就会失效。

**解决**：
- 刷新页面重新获取验证码
- 确保启动后没有再修改 `.env`

### ❌ 搜索结果不对或搜不到

**原因**：搜索只匹配聊天消息的**内容**，不匹配标题。

**解决**：
- 用聊天中出现过的关键词搜索
- 搜索不支持模糊拼音，需要精确中文字符匹配

### ❌ 手机端布局错乱

**原因**：CSS 缓存。

**解决**：
- 强制刷新浏览器（Ctrl+Shift+R 或 Cmd+Shift+R）
- 清除浏览器缓存
- 确认没有自定义 CSS 覆盖

---

## 6. 自定义修改指南

### 6.1 添加新的 AI 提供商

编辑 `main.py`，在 `PROVIDERS` 字典中添加：

```python
"openai": {
    "name": "OpenAI",
    "base_url": "https://api.openai.com/v1",
    "models": ["gpt-4o", "gpt-4o-mini"],
    "default_model": "gpt-4o-mini",
},
```

前端会自动加载新提供商，无需改 HTML。

> ⚠️ 要求提供商的 API 兼容 OpenAI 的 `/chat/completions` 格式。不同提供商的深度思考参数可能不同，需要在 `stream_response()` 函数中适配。

### 6.2 修改端口

三种方式，任选其一：

```bash
# 方式1：环境变量
PORT=8080 python main.py

# 方式2：写在 .env 文件中
echo "PORT=8080" >> .env

# 方式3：Docker 中修改 docker-compose.yml
ports:
  - "8080:3210"
```

### 6.3 修改 Token 有效期

编辑 `main.py` 第 83 行：
```python
JWT_EXPIRE_SECONDS = 30 * 24 * 3600   # 30天，改成你需要的秒数
```

### 6.4 修改最大同时登录设备数

编辑 `main.py` 第 84 行：
```python
MAX_SESSIONS = 5   # 默认5个设备
```

### 6.5 添加密码哈希（推荐）

当前密码是明文存储的。如果要加固，安装 `passlib`：

```bash
pip install passlib[bcrypt]
```

然后在 `main.py` 中修改注册和登录逻辑：
```python
from passlib.hash import bcrypt

# 注册时：存哈希
hashed = bcrypt.hash(password)

# 登录时：验证哈希
bcrypt.verify(password, row["password"])
```

---

## 7. 更新与维护

### 7.1 更新代码

```bash
# 本地或服务器上
git pull origin main

# 如果改了依赖
pip install -r requirements.txt

# 重启服务
sudo systemctl restart aichat   # systemd
# 或
docker-compose down && docker-compose up -d   # Docker
```

### 7.2 备份数据

只需要备份两个东西：

```bash
# 1. 数据库（含用户、Key、聊天记录）
cp data.db data.db.bak.$(date +%Y%m%d)

# 2. 环境变量
cp .env .env.bak.$(date +%Y%m%d)
```

Docker 用户备份 `data/` 目录即可。

### 7.3 升级依赖

```bash
# 查看哪些依赖有新版本
pip list --outdated

# 逐个升级（推荐，避免兼容性问题）
pip install --upgrade fastapi
pip install --upgrade uvicorn

# 生成新的 requirements.txt
pip freeze > requirements.txt
```

### 7.4 清理旧数据

本项目目前没有内置的数据清理功能。如需手动清理：

```bash
# 进入 SQLite
sqlite3 data.db

# 删除 N 天前的消息（保留最近 90 天）
DELETE FROM messages WHERE created_at < unixepoch('now', '-90 days');

# 清理无消息的空对话
DELETE FROM chats WHERE id NOT IN (SELECT DISTINCT chat_id FROM messages);

# 压缩数据库
PRAGMA optimize;
VACUUM;
.quit
```

---

## 📋 部署检查清单

- [ ] Python 版本 ≥ 3.10
- [ ] 已创建 `.env` 并设置了随机 `SECRET_KEY`
- [ ] `pip install -r requirements.txt` 无报错
- [ ] `python main.py` 能正常启动，显示 `Uvicorn running on ...`
- [ ] 浏览器能打开 `http://localhost:3210`
- [ ] 能正常注册账号
- [ ] 在设置页面填入了至少一个提供商的 API Key
- [ ] 能正常发送消息并收到 AI 回复
- [ ] （生产环境）已配置 Nginx 反向代理
- [ ] （生产环境）已配置 HTTPS
- [ ] （生产环境）已配置 systemd 或 supervisord 守护进程
